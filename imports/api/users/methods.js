// Methods
const createAlumnus = (/* obj */) => {
  // conn.sobject('').create({ Name : 'My Account #1' }, function(err, ret) {
    // console.log("Created record id : " + ret.id);
    // Create the user in SF
    // ...
      // Create the alumnus in SF
      // ...
        // Create the Metor user
        // Accounts.createUser({
        //   sfid: '555',
        //   password: 'testtest',
        //   email: this.formData.email
        // });
  // });
};


// Publish
Meteor.methods({
  createAlumnus,
});
