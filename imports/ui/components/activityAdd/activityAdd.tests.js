if (Meteor.isClient) {
  describe('controller', () => {
    // ADD ACTIVITY DETAILS
    it('Not allowed to continue before Name,Date,Activity type entered', () => {
      expect(false).toBe(true);
    });

    it('Should activate CLEAR button when date is selected', () => {
      expect(false).toBe(true);
    });

    it('Button CLEAR (next to date picker) should reset Date', () => {
      expect(false).toBe(true);
    });

    // ADD ALUMNI
    it('If no filters are selected, "CLEAR FILTERS" is not visible', () => {
      expect(false).toBe(true);
    });

    it('Typing text in search decreases text correspondingly', () => {
      expect(false).toBe(true);
    });

    it('Clicking on "Added" will sort alumni by if they are added', () => {
      expect(false).toBe(true);
    });

    it('Clicking "ADD ALL" will toogle of the selects', () => {
      expect(false).toBe(true);
    });

    it('Clicking "ADD NONE" will deselect all of the Alumni', () => {
      expect(false).toBe(true);
    });

    // REVIEW AND SAVE
    it('Edit on the "Activity name" take to "Add activity details"', () => {
      expect(false).toBe(true);
    });

    it('Edit on the "Alumni invited" will take to step "Add alumni"', () => {
      expect(false).toBe(true);
    });

    it('Should save activity when "Save activity" is clicked', () => {
      expect(false).toBe(true);
    });
  });
}
