if (Meteor.isClient) {
  describe('controller', () => {
    it('Entering data in search changes the amount of emails shown', () => {
      expect(false).toBe(true);
    });

    it('Selecting "Sent Mail" shows only outbox', () => {
      expect(false).toBe(true);
    });

    it('When you select "Inbox" to show only emails coming in', () => {
      expect(false).toBe(true);
    });

    it('Should sort mail by the latest', () => {
      expect(false).toBe(true);
    });

    it('check if table has headers', () => {
      expect(false).toBe(true);
    });

    it('Should check that random row has data present', () => {
      expect(false).toBe(true);
    });

    it('Should open buttons Compose message, Email all alumni', () => {
      expect(false).toBe(true);
    });

    it('Should open New email window on click of Fab "Compose email" button', () => {
      expect(false).toBe(true);
    });

    it('Should open email window with all alumni selected on click of Fab', () => {
      expect(false).toBe(true);
    });

    it('Should open dropdown when you click on vertical more', () => {
      expect(false).toBe(true);
    });

    it('Should have tick box shown on hover of image on mail', () => {
      expect(false).toBe(true);
    });

    it('Should have all selected once clicked on select all', () => {
      expect(false).toBe(true);
    });

    // READ MESSAGE ITS
    it('Should open message on click', () => {
      expect(false).toBe(true);
    });

    it('Opened message should have sender', () => {
      expect(false).toBe(true);
    });

    it('Opened message should show message subject', () => {
      expect(false).toBe(true);
    });

    it('Opened message should have content', () => {
      expect(false).toBe(true);
    });

    // NEW MESSAGE ITS
    it('Should have dropdown appear on "To" input', () => {
      expect(false).toBe(true);
    });

    it('Should have "To" input field on a New email popup', () => {
      expect(false).toBe(true);
    });

    it('Should have "Subject" input field on a New email popup', () => {
      expect(false).toBe(true);
    });

    it('Should have "Content" input field on a New email popup', () => {
      expect(false).toBe(true);
    });

    it('Should close popup on click of cross', () => {
      expect(false).toBe(true);
    });
  });
}
