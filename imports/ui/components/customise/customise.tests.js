if (Meteor.isClient) {
  describe('controller', () => {
    // CUSTOMISE ONLINE SIGN UP FORM - NOT DONE
    it('First test', () => {
      expect(false).toBe(true);
    });

    // CUSTOMISE QUESTIONS
    it('Should move question up when clicked on the chevron up', () => {
      expect(false).toBe(true);
    });

    it('Should move question up when clicked on the chevron down', () => {
      expect(false).toBe(true);
    });

    it('Should enter edit mode when clicked on edit', () => {
      expect(false).toBe(true);
    });

    it('Should delete question when clicked on Delete icon (bin)', () => {
      expect(false).toBe(true);
    });

    it('Should allow multiple answers for "Select" or "Multi-Select"', () => {
      expect(false).toBe(true);
    });

    it('After adding "Custom question" it should show on search filter', () => {
      expect(false).toBe(true);
    });
  });
}
