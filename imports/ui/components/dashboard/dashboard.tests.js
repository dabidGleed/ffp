if (Meteor.isClient) {
  describe('controller', () => {
    // Search Alumni
    it('Check if there are alumnus', () => {
      expect(false).toBe(true);
    });

    it('Should have dropdown appear on input', () => {
      expect(false).toBe(true);
    });

    it('Should go to search page after search input', () => {
      expect(false).toBe(true);
    });

    // Registered alumni
    it('Should have total registered alumni inside Total', () => {
      expect(false).toBe(true);
    });

    it('Should have total registered this month inside "This month"', () => {
      expect(false).toBe(true);
    });

    it('Should have total registered this week alumni in "This week"', () => {
      expect(false).toBe(true);
    });

    // Alumni activities
    it('Add activity will open add activity view', () => {
      expect(false).toBe(true);
    });

    // Mailbox
    it('Inbox button will go to inbox', () => {
      expect(false).toBe(true);
    });

    it('Should open New email window on click of "Compose email"', () => {
      expect(false).toBe(true);
    });

    it('Should open New SMS window on click of "Send SMS" button', () => {
      expect(false).toBe(true);
    });

    it('Inbox button will go to inbox', () => {
      expect(false).toBe(true);
    });

    it('Inbox button will go to inbox', () => {
      expect(false).toBe(true);
    });

    // Graph
    it('On "This year" it will all alumni added up', () => {
      expect(false).toBe(true);
    });

    it('Graph has each month', () => {
      expect(false).toBe(true);
    });

    it('Should show Monday to Friday when "This week" is selected', () => {
      expect(false).toBe(true);
    });

    it('When user selects "This month" should have graph', () => {
      expect(false).toBe(true);
    });

    it('On hover of certain part of the graph a rollover should appear', () => {
      expect(false).toBe(true);
    });
  });
}
