if (Meteor.isClient) {
  describe('controller', () => {
    it('Activities have Name', () => {
      expect(false).toBe(true);
    });

    it('Activities have activity type', () => {
      expect(false).toBe(true);
    });

    it('Activities have Date', () => {
      expect(false).toBe(true);
    });

    it('Activities have at least one alumni', () => {
      expect(false).toBe(true);
    });

    // FILTER TABS
    it('"ALUMNI WORKSHOP / ASSEMBLY (SCHOOL LED)" will filter list', () => {
      expect(false).toBe(true);
    });

    it('"OPENING DAY / EVENING" will filter list', () => {
      expect(false).toBe(true);
    });

    it('"IN LESSON SUPPORT" will filter list', () => {
      expect(false).toBe(true);
    });

    it('"ALUMNI REUNION / SOCIAL EVENT" will filter list', () => {
      expect(false).toBe(true);
    });

    it('"E-MENTORING" will filter list', () => {
      expect(false).toBe(true);
    });

    it('"MENTORING" will filter list', () => {
      expect(false).toBe(true);
    });

    it('"WORK EXPERIENCE" will filter list', () => {
      expect(false).toBe(true);
    });

    it('"GOVERNOR" will filter list', () => {
      expect(false).toBe(true);
    });

    it('"FUNDRAISING" will filter list', () => {
      expect(false).toBe(true);
    });

    it('"DONATION" will filter list', () => {
      expect(false).toBe(true);
    });

    it('"OTHER" will filter list', () => {
      expect(false).toBe(true);
    });

    it('"WORKSHOP (FUTURE FIRST LED)" will filter list', () => {
      expect(false).toBe(true);
    });

    it('"ASSEMBLY (FUTURE FIRST LED)" will filter list', () => {
      expect(false).toBe(true);
    });

    it('"WORKSHOP AND ASSEMLY (FUTURE FIRST LED)" will filter list', () => {
      expect(false).toBe(true);
    });

    it('"OTHER EVENT (FUTURE FIRST LED)" will filter list', () => {
      expect(false).toBe(true);
    });

    it('Selecting CREATED BY', () => {
      expect(false).toBe(true);
    });

    it('Entering text in search field filters the activities', () => {
      expect(false).toBe(true);
    });

    it('Pressing add activity icon will open add activity view', () => {
      expect(false).toBe(true);
    });

    it('Pressing edit icon will open that activity in edit mode', () => {
      expect(false).toBe(true);
    });

    it('Pressing remove icon open confirmation to delete that activity', () => {
      expect(false).toBe(true);
    });

    // UPCOMING ACTIVITY
    it('Clicking on Alumni added > X will show you the list of Added', () => {
      expect(false).toBe(true);
    });

    it('Clicking on Attending > X will show you the list of Attending', () => {
      expect(false).toBe(true);
    });

    it('Clicking on Awaiting response > X will show you the list await', () => {
      expect(false).toBe(true);
    });

    it('Clicking on Declined > X will show you the list of declined', () => {
      expect(false).toBe(true);
    });

    it('Clicking on Maybe > X will show you the list of maybe', () => {
      expect(false).toBe(true);
    });

    // PAST ACTIVITIES
    it('Clicking on Attended > X will show you the list attended', () => {
      expect(false).toBe(true);
    });

    it('Clicking on No-show > X will show you the list of no show', () => {
      expect(false).toBe(true);
    });
  });
}
